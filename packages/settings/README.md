# @bight-ts/settings

Service-first settings helpers for Bight.ts.ts storage

## Install

```bash
pnpm add @bight-ts/settings
```

## Package Role

Use this package when you want a service-first settings layer on top of Bight.ts storage.

Published from the `bight-ts-extras` monorepo.
