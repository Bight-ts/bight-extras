# @bight-ts/plugin-prefix-commands

Optional prefix-command plugin for Bight.ts.ts

## Install

```bash
pnpm add @bight-ts/plugin-prefix-commands
```

## Package Role

Use this package when you want to add an optional runtime capability to a Bight.ts app through explicit plugin registration.

Published from the `bight-ts-extras` monorepo.
